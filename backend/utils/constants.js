const DEFAULT_MEAL_ORDER = [
  'breakfast',
  'lunch',
  'snack',
  'dinner',
];

const MENU_TYPE_DAYS = {
  weekly: 7,
  biweekly: 14,
  monthly: 30,
};

module.exports = {
  DEFAULT_MEAL_ORDER,
  defaultMealOrder: DEFAULT_MEAL_ORDER,
  MENU_TYPE_DAYS,
  menuTypeDays: MENU_TYPE_DAYS,
};